# State Machine — {{critical-flow-name}}

> **How to use:** one file (or section) per critical flow with its own lifecycle (order,
> subscription, ticket, return…), instantiated by `agents/01-requirements/business-rules-modeler.md`
> in F5, with `modules/state-machines.md` as the method. Lives at
> `product/04-specification/state-machines.md` (one section per flow; large projects use one file
> per flow at `product/04-specification/state-machines/{{flow-name}}.md`).

> **Status:** draft
> **Owner:** `agents/01-requirements/business-rules-modeler.md`
> **Phase:** F5 · **Updated:** {{yyyy-mm-dd}}
> **Satisfies:** {{fr-nnn}}, {{br-nnn}}
> **Consumers:** Backend (F6), `agents/05-backend/authorization-specialist.md`, tests (F6/F7)

## Modeled entity

*Which entity has this lifecycle and why it needs an explicit state machine instead of a free-form
field — usually because wrong transitions corrupt data or the business.*

{{entity-name}} — {{reason-it-is-a-critical-flow}}

## States

| State | Description | Terminal? |
| --- | --- | --- |
| {{state-name}} | {{what-being-in-this-state-means}} | {{yes/no}} |

*A terminal state has no outgoing transitions — if it seems to need one, that is a sign a state is
missing or it is not terminal.*

## Diagram

*Mermaid (versionable, readable by agents). Keep it in sync with the transitions table below — the
diagram is a view, the table is the source of truth.*

```mermaid
stateDiagram-v2
    [*] --> {{initial-state}}
    {{initial-state}} --> {{next-state}}: {{transition-name}}
    {{next-state}} --> [*]
```

## Transitions table

*Every transition has its own name (used in code and logs, not "change state"). "Who can" is
authority (`modules/rbac-and-scoping.md`), not scoping. The guard is the precondition checked on the
server before accepting the transition.*

| Transition | From | To | Who can | Guard (precondition) | Effects |
| --- | --- | --- | --- | --- | --- |
| {{transition-name}} | {{source-state}} | {{target-state}} | {{role-or-actor}} | {{condition-that-must-be-true}} | {{what-else-changes-in-the-system}} |

## Invalid transitions

*As explicit as the valid ones — the server must reject them (never ignore silently), and they are
the basis for the rejection criterion in `templates/specification/functional-requirement.md.template`.*

| From | To (attempted) | Why it is invalid | Server response |
| --- | --- | --- | --- |
| {{source-state}} | {{forbidden-target-state}} | {{reason}} | {{error-or-code-returned}} |

## Terminal states

- {{terminal-state-1}} — {{what-it-prevents-from-here-on}}

## Base + overlay (if applicable)

*Only when a temporary action competes with permanent state for the same field (e.g. a loan cannot
erase the base owner). If it does not apply, write "Not applicable — no temporary state competing
with permanent state".*

{{description-of-the-temporary-layer-and-how-it-reverts-to-base}}

## Concurrency notes

*Two simultaneous transitions on the same instance — which lock/transaction prevents the
inconsistent state (TOCTOU). If there is no relevant concurrency risk, say so explicitly.*

{{lock-or-transaction-note-when-applicable}}

## Related

- `modules/state-machines.md` · `agents/01-requirements/business-rules-modeler.md`
- `agents/05-backend/authorization-specialist.md` · `agents/06-data/data-modeler.md`
- `templates/specification/business-rules.md.template` · `templates/specification/backend-contract.md.template`
- `workflows/W05-specification.md` · `knowledge/origin-lessons.md`
