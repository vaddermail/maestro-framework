# Logical Data Model — {{domain-or-module-name}}

> **How to use:** instantiated by `agents/06-data/data-modeler.md` in F5, once business rules and
> state machines exist. Lives at `product/04-specification/logical-data-model.md`.
> **DB-engine agnostic** — no concrete physical types (that is the derivation to the physical model
> in F6, after the stack is decided); here you describe entities, relationships and invariants.

> **Status:** draft
> **Owner:** `agents/06-data/data-modeler.md`
> **Phase:** F5 (logical) · F6 (physical derivation) · **Updated:** {{yyyy-mm-dd}}
> **Satisfies:** {{br-nnn-this-model-enforces}}
> **Consumers:** `migration-engineer`, `indexing-specialist`, backend (F6), reviewers

## Entities

*One line per entity, canonical names from `product/01-requirements/glossary.md`. Attribute detail
goes in the next section.*

| Entity | Description | Essential attributes |
| --- | --- | --- |
| {{entity-name}} | {{what-it-represents}} | {{short-list-of-defining-attributes}} |

## Attributes per entity

*Repeat per entity. "Required?" decides `NOT NULL` explicitly — do not leave the
NULL-means-absent vs. NULL-means-unknown ambiguity unresolved.*

### {{entity-name}}

| Attribute | Logical type | Required? | Notes |
| --- | --- | --- | --- |
| {{attribute-name}} | {{text \| number \| date \| boolean \| enumeration \| reference}} | {{yes/no}} | {{format-precision-rule-or-source-catalog}} |

## Relationships

*A bidirectional relationship stores one side as the source; the other is derived by query — never
both as editable columns in parallel. Always mark the canonical side.*

| Source | Relationship | Target | Cardinality | Canonical side | Notes |
| --- | --- | --- | --- | --- | --- |
| {{entity-a}} | {{relationship-name}} | {{entity-b}} | {{1:1 \| 1:N \| N:M}} | {{entity-holding-the-fk-or-association-table}} | {{e.g.-history-with-start-end-instead-of-plain-fk}} |

## Lifecycles as history

*Whenever an entity on this list has state that changes (see `templates/specification/state-machine.md.template`),
model it as history with `start`/`end` — the "current" one is the record without `end`, never an
overwritten field. If there are no relevant lifecycles here, say so.*

| Entity | History table | What the "current" record represents |
| --- | --- | --- |
| {{entity}} | {{assignment-or-history-table-name}} | {{record-without-end-or-equivalent}} |

## Cross-entity invariants

*Numbered catalog — each invariant is a candidate for a DB constraint, with the prescription for how
it is enforced and how it is tested per named violation.*

| ID | Statement | Why | Enforcement prescription |
| --- | --- | --- | --- |
| {{i-nnn}} | {{fact-that-can-never-be-false}} | {{consequence-of-violating-or-origin-defect}} | {{check \| partial-unique \| fk \| not-null + app-guard}} |

## Reference seeds

*Catalogs (statuses, categories, priorities) live in configurable tables, not in code enums. Demo
seed dates are relative to an anchor (`today − N days`), never absolute — so the demo does not age.*

| Catalog | Reference values | Date anchor (if applicable) |
| --- | --- | --- |
| {{catalog-name}} | {{list-of-values}} | {{e.g.-reference-date-minus-n-days}} |

## Related

- `agents/06-data/data-modeler.md` · `agents/01-requirements/business-rules-modeler.md`
- `agents/06-data/migration-engineer.md` · `agents/06-data/indexing-specialist.md`
- `templates/specification/business-rules.md.template` · `templates/specification/state-machine.md.template`
- `knowledge/proven-patterns.md` · `workflows/W05-specification.md`
