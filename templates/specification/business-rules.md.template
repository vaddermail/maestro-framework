# Business Rules — {{module-name}}

> **How to use:** one file per module (or the project's full catalog, in small prototypes),
> instantiated by `agents/01-requirements/business-rules-modeler.md` in F2, from the `FR`s
> that presuppose rules. Lives at `product/01-requirements/business-rules.md`. Each `BR` is numbered
> and **eternal**: if it dies, it is marked `obsolete`; the ID is never reused.

> **Status:** draft
> **Owner:** `agents/01-requirements/business-rules-modeler.md`
> **Phase:** F2 (sketch) · F5 (consolidation) · **Updated:** {{yyyy-mm-dd}}
> **Satisfies:** {{fr-nnn-that-presuppose-these-rules}}
> **Consumers:** `acceptance-criteria-writer`, `data-modeler`, `authorization-specialist`, reviewers, tests

## Module overview

*Two sentences: which part of the domain this module covers and why its rules live together (e.g.
"subscription billing" or "assigning assets to employees").*

{{brief-module-description}}

## Rule catalog

*One line per rule, for a quick view. The detail of each one goes in the next section.*

| ID | Type | Statement (summary) | Where it applies |
| --- | --- | --- | --- |
| {{br-nnn}} | invariant \| decision rule \| constraint | {{one-line-summary}} | client \| server \| DB |

## Detail per rule

*Repeat this block per `BR`. A statement that needs "and/or" to join two independent ideas is a
candidate for two rules.*

### {{br-nnn}} — {{short-title}}

| Field | Value |
| --- | --- |
| **Type** | {{invariant \| decision-rule \| constraint}} |
| **Verifiable statement** | {{testable-sentence-without-ambiguity}} |
| **Why (provenance)** | {{business-decision-or-defect-that-originated-it}} |

**Where it applies and is enforced** — *a hard rule lives in more than one layer (defense in depth);
mark all that apply, never trust the app alone.*

| Layer | Applies? | Mechanism |
| --- | --- | --- |
| Client | {{yes/no}} | {{ux-friendly-guard}} |
| Server | {{yes/no}} | {{guard-in-orchestration-or-authz}} |
| Database | {{yes/no}} | {{constraint-check-partial-unique-fk}} |

**Explicit exceptions** — *a rule with no recorded exceptions is assumed to have none; an exception
not written here does not exist.*

- {{exception-condition}} → {{different-behavior}}. *(if there are no exceptions: "None — always applies".)*

**Related** — `FR`: {{fr-nnn}} · `BR`: {{related-br-nnn}} · State machine: {{flow-name, if applicable}}

---

## Modeling notes

*Where three mechanisms intersect (eligibility gate, authorization, tier proportional to a value),
record the boundary between them here instead of collapsing them into a single rule — see
`modules/approval-engine.md`. Thresholds (values, deadlines) are never fixed in the rule statement;
they live in a configurable catalog referenced here.*

## Related

- `agents/01-requirements/business-rules-modeler.md` · `agents/01-requirements/requirements-engineer.md`
- `agents/06-data/data-modeler.md` · `agents/05-backend/authorization-specialist.md`
- `modules/state-machines.md` · `modules/approval-engine.md` · `modules/rbac-and-scoping.md`
- `templates/specification/state-machine.md.template` · `templates/specification/functional-requirement.md.template`
- `knowledge/origin-lessons.md`
