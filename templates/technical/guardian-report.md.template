# Guardian Report — `{{dimension}}`

> **How to use:** copy to `product/99-records/guardians/{{dimension}}-YYYY-MM-DD.md`, one file
> per cycle. All of `agents/13-guardians/` uses this template. A report without numbers and without
> terminal states **does not close the cycle** — format defined in
> `agents/13-guardians/README.md` §Cycle report format.

## Header

| Field | Value |
| --- | --- |
| Guardian (dimension) | {{e.g.-security-guardian}} |
| Cycle window | {{start-date}} to {{end-date}} |
| Cadence | {{daily-weekly-monthly-per-event}} |
| Previous cycle | `{{path-to-previous-report}}` |

## Findings by terminal state

*Every finding of the cycle ends resolved, mitigated (residual risk accepted by the user) or
not-applicable (justified). Nothing stays "under analysis" without an owner and a deadline.*

### Resolved

| Finding | Action applied | Validation |
| --- | --- | --- |
| {{e.g.-cve-2026-x-in-lib-y}} | {{patch-applied}} | {{regression-test-green}} |

### Mitigated (residual risk accepted)

| Finding | Mitigation | Accepted by | Date | Planned review |
| --- | --- | --- | --- | --- |
| {{finding}} | {{mitigation-applied}} | {{user}} | {{yyyy-mm-dd}} | {{when-to-reassess}} |

### Not-applicable

| Finding | Why it does not apply |
| --- | --- |
| {{finding}} | {{justification}} |

## Actions applied and how they were validated

*Never "applied" without proof — the cycle is analyze→plan→apply→validate→document.*

| Action | Real validation (never "looks fine") |
| --- | --- |
| {{action}} | {{concrete-evidence}} |

## What was escalated to the user

*Only the user accepts residual risk and decides scope/money/data/production — the guardian
recommends, it does not decide (`MANIFESTO.md` §8).*

- {{pending-decision-1-with-options-and-default-recommendation}}

## Trend versus the previous cycle

| Metric | Previous cycle | This cycle | Direction |
| --- | --- | --- | --- |
| {{e.g.-number-of-open-findings}} | {{value}} | {{value}} | {{better-worse-same}} |

## Related

- `agents/13-guardians/README.md` · `core/lifecycle.md`
- `workflows/W09-continuous-operation.md` · `loops/README.md`
- `core/project-memory.md`
