# Review Report — `{{reviewer-dimension}}`

> **How to use:** copy to `product/99-records/reviews/{{dimension}}-{{slice}}-YYYY-MM-DD.md`. Every
> reviewer in `agents/12-reviewers/` uses this template **without changes**, so the
> `agents/12-reviewers/review-consolidator.md` can merge them without translation. Format defined in
> `agents/12-reviewers/README.md` §Report format.

## 1. Header

| Field | Value |
| --- | --- |
| Reviewer (dimension) | {{e.g.-backend-reviewer}} |
| Scope reviewed | {{slice-commits-or-artifacts-reviewed}} |
| Date | {{yyyy-mm-dd}} |
| Model and effort used | {{e.g.-top-effort-high}} (`core/model-routing.md`) |

## 2. Overall verdict

*A single blocker is enough to block (`core/quality-gates.md`).*

**{{pass | pass-with-reservations | block}}**

{{one-sentence-justification-of-the-verdict}}

## 3. Findings (ordered by severity)

*One confirmed finding is worth more than ten suspicions — order by real risk, not by number of
findings. Findings on correctness, authorization, money, personal data and irreversible flows get
maximum scrutiny (`MANIFESTO.md` §9).*

| ID | Severity | Location | Defect (one sentence) | Concrete failure scenario | Recommendation | Confidence |
| --- | --- | --- | --- | --- | --- | --- |
| A1 | {{blocker \| major \| minor \| nit}} | `{{file:line-or-artifact}}` | {{defect-in-one-sentence}} | {{inputs-state-wrong-outcome-never-looks-fragile}} | {{concrete-recommendation}} | {{confirmed \| plausible}} |

## 4. What was checked and passed

*Not just the negative — builds confidence and tells the consolidator what is already covered.*

- {{checked-item-1}}
- {{checked-item-2}}

## 5. Out of scope / not verifiable

*Absolute honesty (`knowledge/permanent-rules.md` §2): what this reviewer did not look at and
why — missing artifact or another reviewer's dimension.*

| What was not checked | Why |
| --- | --- |
| {{e.g.-performance-under-load}} | {{another-reviewer's-dimension-or-missing-artifact}} |

## Related

- `agents/12-reviewers/README.md` · `agents/12-reviewers/review-consolidator.md`
- `checklists/pr-review.md` · `checklists/pre-merge.md` · `core/quality-gates.md`
